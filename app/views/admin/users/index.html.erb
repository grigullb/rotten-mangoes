<h1>All users</h1>
<%= paginate @users %>
<%@users.each do |user|%>
	<p><%= link_to user.full_name, admin_user_path(user)%> (<%= link_to "delete", admin_user_path(user), method: :delete, data: {confirm: "you sure?"} %>)</p>
<% end %>
